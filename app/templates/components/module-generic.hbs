a generic module of type {{model.type}}
with ports:

{{#each model.ports as |port|}}{{port-component  model=port  
                  select=(action 'selectPort' port) }}{{/each}}
<button {{action 'remove'}}>Remove Module</button>