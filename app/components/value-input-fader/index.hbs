<div class={{this.faderClasses}} style={{this.inlineStyles}}>
  <RangeSlider
    @value={{@item.value}}
    @min={{@min}}
    @max={{@max}}
    @step={{@step}}
    @displayScale={{@displayScale}}
    @onChange={{this.updateValue}}
  />
</div>