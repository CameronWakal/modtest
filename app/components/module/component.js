import Component from '@glimmer/component';

// Base module component - uses ModuleWrapper for common functionality
// Subclasses can extend this or use ModuleWrapper directly in their templates
export default class ModuleComponent extends Component {
}
